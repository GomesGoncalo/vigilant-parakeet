// wasm integration tests removed; wasm-bindgen tests now live inside the crate
// under cfg(target_arch = "wasm32").

// Intentionally empty to avoid unresolved-crate issues during wasm test builds.
